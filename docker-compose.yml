services:
  chroma:
    image: chromadb/chroma
    ports:
      - "8000:8000"
    volumes:
      - ./data/chroma:/chroma/chroma
    environment:
      - ANONYMIZED_TELEMETRY=False
      - CHROMA_SERVER_PERSIST_DIR=/chroma/chroma
      - ALLOW_RESET=TRUE
    restart: unless-stopped